name: login_checkin

on:  [ push ]
jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: python_env
        uses: actions/setup-python@v1
        with:
          python-version: 3.6
      - name: 'run'
        run: python ./xmrth_checkin.py

